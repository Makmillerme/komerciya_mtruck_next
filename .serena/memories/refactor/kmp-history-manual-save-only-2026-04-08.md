## KMP історія: лише явне збереження (2026-04-08)

- **Проблема:** автозбереження після перезавантаження (чернетка + валідна форма) знову викликало POST і дубль у `kmp-history.json`.
- **Рішення:** автозбереження прибрано. Запис у історію лише по кнопці **«Зберегти в історію»** у `KmpHistoryToolbar`.
- **UX:** `lastSavedKmpFingerprint` (`useState`) оновлюється після успішного POST і при `loadKmpHistoryEntry`; обчислюється `currentKmpFingerprint` (`useMemo`). Кнопка активна лише коли форма валідна, є `loanParsed`, відбиток змінився від останнього збереженого/завантаженого знімка, немає активного `savePending`.
- Файл відбитків: `lib/kmp-history-fingerprint.ts`.
